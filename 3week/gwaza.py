price = 2650
paid = 10000
change = [0, 0, 0, 0, 0]  # 5000, 1000, 500, 100, 50

diff = paid - price

while diff > 0:
    if diff >= 5000:

    elif diff >= 1000:
    elif diff >= 500:
    elif diff >= 100:
    elif diff >= 50:

    pass
